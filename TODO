COMMENT BUFFER OVERFLOW PRINT ERROR AND EXIT

BONUS:
  na kratame se poio line eimaste
  flag gia to oti den ekleise string
  comments


ston scanner.l stin insert_data:
  opu exoume if(type==CONST_CHAR_*_LITERAL) na to kanume if(!strcmp(type,CONST_CHAR_*_LITERAL))